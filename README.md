# Installation

`cp .env.example .env`

`cp .env.example .env.test`

# Backend
`go run main.go` run go server

# UI for tests
`sudo $GOPATH/bin/goconvey`

# Frontend
`cd frontend`

`npm install`

`npm run format`

`npm run dev`
